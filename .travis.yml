language: c

compiler:
  - gcc
  - clang

before_script:
  - echo 'yes' | sudo add-apt-repository ppa:ubuntu-toolchain-r/test
  - sudo apt-get update

script:
  - ./build/init_autotools.sh
  - ./configure        
  - make
