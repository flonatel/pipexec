# Defaults for pipexec initscript
# sourced by /etc/init.d/pipexec
# installed at /etc/default/pipexec by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
