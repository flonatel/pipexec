---

name: CompileCheck

on: [push]

jobs:
  compile-gcc-10:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          bash -x build/init_autotools.sh
          mkdir -p COMPILE
          cd COMPILE
          ../configure
          make
        shell: bash
